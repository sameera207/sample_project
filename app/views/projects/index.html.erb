<h1>Listing projects</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
  </tr>

<% @projects.each do |project| %>
  <tr>
    <td><%= project.name %></td>
    <td><%= link_to 'Show', project %></td>
  </tr>
<% end %>
</table>

